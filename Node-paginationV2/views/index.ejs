<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NODEJS PAGİNATİON</title>
    <link rel="stylesheet" href="style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container">
      <div class="row mt-3">
        <% result.forEach(element => { %>
        <div class="col-sm-6 mb-3 mb-sm-0">
          <div class="card mt-3">
            <div class="card-body">
              <h5 class="card-title"><%=element.title %></h5>
              <p class="card-text"><%=element.description %></p>
              <a href="#" class="btn btn-outline-primary">Detay Gör</a>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
    </div>

    <div class="container mt-3">
      <nav aria-label="Page navigation example ">
        <ul class="pagination">
            <% if (nowPage == 1) { %>
                <li class="page-item active"><a class="page-link" href="?page=1">First</a></li>
            <% } else { %>
                <li class="page-item"><a class="page-link" href="?page=1">First</a></li>
            <% } %>
        
            <!--  ara sayfaların yazımı -->
             <% for (let i = 1; i <= totalPage; i++) { %>
                <% if (nowPage == i) { %>
                    <li class="page-item active"><a class="page-link" href="?page=<%= i %>"><%= i %></a></li>
                <% } else { %>
                    <li class="page-item"><a class="page-link" href="?page=<%= i %>"><%= i %></a></li>
                <% } %>
            <% } %> 
        
            <% if (nowPage == totalPage) { %>
                <li class="page-item active"><a class="page-link" href="?page=<%= totalPage %>">Last</a></li>
            <% } else { %>
                <li class="page-item"><a class="page-link" href="?page=<%= totalPage %>">Last</a></li>
            <% } %>
        </ul>
        
      </nav>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
